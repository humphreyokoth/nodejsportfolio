/**
 * When the static site is on Firebase (or similar) and the API is on Railway, set this to your Web service public HTTPS URL (no trailing slash).
 * Placeholder → Contact and Meals show a setup hint; /api/* on the static host will 404.
 */
export const API_BASE = "https://nodejsportfolio-production.up.railway.app";
